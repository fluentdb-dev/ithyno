{
  "name": "@ithyno/hub",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "npm run --workspace @ithyno/shared build && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "cd .. && vitest run --config vitest.config.ts hub/**/*.test.ts"
  },
  "dependencies": {
    "@ithyno/shared": "file:../shared",
    "fastify": "^5.2.1",
    "sqlite3": "^6.0.1",
    "ws": "^8.18.1"
  }
}
