## 1. Implementation

- [x] 1.1 Add `sendTheme()` helper, `load`-event retry loop, and MutationObserver to `renderOnboardingHtml`
- [x] 1.2 Handle `vscode:get-theme` in the onboarding message handler before checking for `onboarding-open/close`
- [x] 1.3 Build `out/extension.js` and verify no type errors
