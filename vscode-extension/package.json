{
  "name": "ithyno-vscode",
  "displayName": "ithyno",
  "description": "ithyno dashboard for the OpenSpec workflow, embedded in VS Code as a webview panel.",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-or-later",
  "publisher": "ithyno",
  "engines": {
    "vscode": "^1.85.0"
  },
  "main": "./out/extension.js",
  "activationEvents": [
    "onCommand:ithyno.show"
  ],
  "contributes": {
    "commands": [
      {
        "command": "ithyno.show",
        "title": "ithyno: Show Dashboard",
        "category": "ithyno"
      }
    ],
    "configuration": {
      "title": "ithyno",
      "properties": {
        "ithyno.terminalStartup": {
          "type": "string",
          "default": "claude --continue",
          "description": "Command auto-launched when the ithyno terminal is created (so slash commands land in Claude's REPL). Set to an empty string to disable and stay in the raw shell."
        }
      }
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "prepack:host": "node scripts/prepack.mjs",
    "package": "npm run build && npm run prepack:host && vsce package --no-dependencies --allow-missing-repository --out ithyno.vsix"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.2.1",
    "typescript": "^5.7.3"
  }
}
